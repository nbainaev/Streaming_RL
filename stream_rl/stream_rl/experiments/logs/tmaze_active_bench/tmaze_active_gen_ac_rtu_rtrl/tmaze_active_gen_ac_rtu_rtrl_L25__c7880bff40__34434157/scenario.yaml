# Auto-generated for corridor_length=25 by
# gen_tmaze_gen_configs.py -- freeze/eval trigger steps are scaled to
# this training leg's length (84000 steps = 3000 episodes
# at this corridor_length's episode_length), so every corridor_length in
# the grid gets the same number of training episodes before eval.
#
# Timeline (total_timesteps must be 109000):
#   0 - 84000: train at corridor_length=25
#   84000 - 89000: eval @ 100
#   89000 - 94000: eval @ 300
#   94000 - 99000: eval @ 500
#   99000 - 104000: eval @ 700
#   104000 - 109000: eval @ 1000
type: event
events:
  - at_step: 84000
    action: freeze_agent
  - at_step: 84000
    action: change_setup
    params:
      env_overrides:
        kwargs:
          corridor_length: 100
  - at_step: 89000
    action: change_setup
    params:
      env_overrides:
        kwargs:
          corridor_length: 300
  - at_step: 94000
    action: change_setup
    params:
      env_overrides:
        kwargs:
          corridor_length: 500
  - at_step: 99000
    action: change_setup
    params:
      env_overrides:
        kwargs:
          corridor_length: 700
  - at_step: 104000
    action: change_setup
    params:
      env_overrides:
        kwargs:
          corridor_length: 1000

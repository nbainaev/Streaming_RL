agent_config: ../agents/stream_ac_frozen_ssm_dense_adapter_rtu_rtrl.yaml
env_config: ../envs/tmaze_passive_curriculum.yaml
scenario_config: ../scenario/tmaze_passive_memory_curriculum.yaml

experiment_name: autoresearch_memory_classic_tmaze
run_id: stream_frozen_ssm_dense_adapter_rtu_curriculum_60k
log_root: logs/autoresearch_memory_classic_tmaze
total_timesteps: 60000
log_every: 2000
# Exact RTRL creates a very large compiled graph at chunk=1000 for this
# stacked model.  A smaller service chunk changes dispatch granularity only,
# not the one-transition update rule or the total interaction budget.
step_chunk: 100
checkpoint_every: 20000
eval_every: -1
eval_steps: -1

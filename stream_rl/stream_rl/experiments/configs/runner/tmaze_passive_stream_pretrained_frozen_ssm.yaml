agent_config: ../agents/stream_ac_pretrained_frozen_ssm.yaml
env_config: ../envs/tmaze_passive.yaml
scenario_config: null

experiment_name: pretrained_frozen_memory_passive_tmaze
run_id: stream_pretrained_frozen_ssm_concat
log_root: logs/pretrained_frozen_memory_passive_tmaze
total_timesteps: 20000
log_every: 3000
step_chunk: 1000
checkpoint_every: -1
eval_every: -1
eval_steps: -1
